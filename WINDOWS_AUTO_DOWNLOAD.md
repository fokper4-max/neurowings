# Автоматическое скачивание .exe на Windows

## 🚀 Быстрый старт

### Способ 1: Двойной клик (самый простой)

1. Скачайте файл `download_build.bat` на Windows
2. Двойной клик по файлу
3. Скрипт автоматически отследит сборку и скачает .exe
4. Готовый файл будет в папке `build_output\NeuroWings-Windows\`

### Способ 2: PowerShell

1. Откройте PowerShell
2. Перейдите в папку с проектом:
   ```powershell
   cd C:\путь\к\neurowings_recovere
   ```
3. Запустите скрипт:
   ```powershell
   .\download_build.ps1
   ```

---

## 📋 Что делает скрипт:

✅ Проверяет статус сборки каждые 30 секунд
✅ Показывает прогресс в реальном времени
✅ Автоматически скачивает готовый .exe когда сборка завершится
✅ Распаковывает архив
✅ Показывает путь к готовому файлу

---

## ⏱️ Время работы:

- **Сборка**: ~10-15 минут
- **Проверка**: каждые 30 секунд
- **Максимальное ожидание**: 30 минут

---

## 📁 Результат:

Готовые файлы сохраняются в:
```
build_output\
  └── NeuroWings-Windows\
      └── NeuroWings.exe  ← Ваш файл!
```

---

## 🔧 Если скрипт не запускается:

### Ошибка: "Execution Policy"

Откройте PowerShell **от администратора** и выполните:
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

Потом запустите скрипт снова.

### Альтернатива: Запуск без изменения политики

```powershell
powershell.exe -ExecutionPolicy Bypass -File .\download_build.ps1
```

---

## 🌐 Ручное скачивание (если скрипт не работает):

1. Откройте: https://github.com/fokper4-max/neurowings/actions
2. Выберите последнюю успешную сборку (зелёная галочка ✓)
3. Прокрутите вниз до **Artifacts**
4. Скачайте **NeuroWings-Windows.zip**
5. Распакуйте и запустите `.exe`

---

## 💡 Совет:

После первой успешной сборки можете отключить консоль в `.spec` файле:
- Измените `console=True` на `console=False`
- Сделайте `git push`
- Скрипт автоматически скачает новую версию без консоли!
