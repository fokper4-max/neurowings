# Быстрый старт: Автоматическая сборка .exe

## Что я сделал для вас:

✅ Исправил `NeuroWings.spec` - теперь правильно собирает PyTorch DLL
✅ Создал GitHub Actions workflow - автоматическая сборка на серверах GitHub
✅ Создал `requirements.txt` со всеми зависимостями
✅ Настроил .gitignore

---

## Что нужно сделать (5 минут):

### 1. Создайте репозиторий на GitHub

Зайдите на [github.com](https://github.com) и создайте новый репозиторий (например, `neurowings`)

### 2. Загрузите код на GitHub

```bash
cd /Users/dubrovskiy.dda/Downloads/neurowings_recovere

# Инициализация (если ещё не сделано)
git init
git add .
git commit -m "Initial commit with auto-build"

# Подключение к GitHub (замените YOUR_USERNAME на свой username)
git remote add origin https://github.com/YOUR_USERNAME/neurowings.git
git push -u origin main
```

### 3. Запустите сборку

1. Откройте `https://github.com/YOUR_USERNAME/neurowings`
2. Нажмите вкладку **Actions**
3. Выберите **Build Windows EXE**
4. Нажмите **Run workflow** → зелёную кнопку **Run workflow**

### 4. Скачайте готовый .exe

Через ~10-15 минут сборка завершится:
1. В Actions найдите завершённую сборку (зелёная галочка ✓)
2. Прокрутите вниз до секции **Artifacts**
3. Скачайте **NeuroWings-Windows.zip**
4. Распакуйте и запустите `NeuroWings.exe`

---

## Автоматизация

Теперь **при каждом `git push`** GitHub автоматически соберёт новый .exe!

Больше не нужно перекидывать файлы на Windows вручную 🎉

---

## Если хотите вручную собрать на Windows

Просто скопируйте обновлённый `NeuroWings.spec` на Windows и выполните:

```bash
pyinstaller NeuroWings.spec --clean
```

Готовый .exe будет в папке `dist/`

---

## Проблемы?

Если сборка на GitHub упадёт с ошибкой:
1. Откройте упавший workflow в Actions
2. Посмотрите логи (красный текст)
3. Пришлите мне скриншот - я помогу исправить
